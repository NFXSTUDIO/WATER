function openGame(game) {
    // Remplacez cette ligne par votre logique d'ouverture de la page du jeu
    window.open(`games/${game}/${game}.html`, '_blank');
}

function openGame2(game,page) {
    // Remplacez cette ligne par votre logique d'ouverture de la page du jeu
    window.open(`games/${game}/${page}.html`, '_blank');
}